<ul>
  <% @tasks.each do |task| %>
    <li><h2><%= link_to task.title, task_path(task) %> -
    <%= link_to "🚮", task_path(task), data: {turbo_method: :delete, turbo_confirm: 'Are you sure you want to delete this task ?'}%>
</h2></li>

  <% end %>
<p><%= link_to " ➕ Add new task", task_new_path %></p>
</ul>
